
export default function MainRepeatButton({ name='Register' }) {
    return (
        <div className="main-repeat-button-container">
            <button className="main-repeat-button">{name}</button>
        </div>
    )
}